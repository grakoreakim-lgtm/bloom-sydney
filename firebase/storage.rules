rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Product photos: anyone can read, only signed-in admin can write/delete.
    match /products/{file=**} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024  // 5 MB max
                   && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null;
    }

    // Blog cover images: public read, admin-only write/delete.
    match /blog/{file=**} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null;
    }

    // Review photos: public read, admin-only write/delete.
    match /reviews/{file=**} {
      allow read: if true;
      allow write: if request.auth != null
                   && request.resource.size < 5 * 1024 * 1024
                   && request.resource.contentType.matches('image/.*');
      allow delete: if request.auth != null;
    }
  }
}
